{/*
import { Router } from 'express';
import { container } from '../../inversify.config'; // or wherever your DI container is
import { GetAssessmentListUseCase } from '../application/features/assessments/getList/useCase';
import { getAssessmentListController } from '../application/features/assessments/getList/controller';

const router = Router();
const useCase = container.get(GetAssessmentListUseCase);

router.get('/list', getAssessmentListController(useCase));

export default router;
*/}